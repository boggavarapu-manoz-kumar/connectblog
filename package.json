{
  "name": "connectblog-root",
  "version": "1.0.0",
  "description": "Root configuration for monorepo deployments on Railway and Render",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "install-backend": "cd backend && npm install",
    "build": "npm run install-backend",
    "start": "cd backend && npm start"
  }
}

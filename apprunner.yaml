version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - cd backend && npm ci --only=production
run:
  runtime-version: 18
  command: node backend/server.js
  network:
    port: 5000
    env: PORT
  env:
    - name: NODE_ENV
      value: production
